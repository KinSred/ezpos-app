# General Rules

- **Node.js Environment**: Node.js is installed locally in `$HOME/.local/node/bin`. Whenever running `npm`, `npx` or `node` commands, ensure you prefix the command with `export PATH="$HOME/.local/node/bin:$PATH" && ` to ensure the correct environment is used.
